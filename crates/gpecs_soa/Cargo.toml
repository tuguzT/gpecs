[package]
name = "gpecs_soa"
description = "Structure of Arrays (SoA) implementation for (probably) ECS on GPU"
version.workspace = true
license.workspace = true
authors.workspace = true
edition.workspace = true

[dependencies]
gpecs_utils = { workspace = true }

[dev-dependencies]
criterion = { workspace = true }

[[bench]]
name = "soa"
harness = false
